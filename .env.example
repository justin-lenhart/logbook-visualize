# Copy to .env (gitignored) and fill in. Never commit .env.
# Tailscale IP of mintbox — Metabase binds to this IP only.
TS_IP=100.78.241.102
# Metabase admin account (created by scripts/metabase_setup.py).
MB_ADMIN_EMAIL=whoostie@gmail.com
MB_ADMIN_PASSWORD=
